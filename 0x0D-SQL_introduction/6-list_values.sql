-- script lisitng all rows of table first_table from DB hbtn_0c_0 in MySQL server
-- All fields should be printed
-- The DB name will be passed as an argument of the mysql command

SELECT * FROM first_table;
